import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  card: {
    alignItems: 'center',
    paddingVertical: 10,
    overflow: 'hidden',
  },
  icon: {
    position: 'absolute',
    top: 0,
    right: 0,
  },
});
